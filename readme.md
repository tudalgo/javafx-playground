# JavaFXPlayground

A simple JavaFX gradle-Project to play with JavaFX.

## Requirements
- JDK 17 installation

## How to run
the project can be run via the command line:
- Unix-based systems:
```bash
./gradlew run
```
- Windows:
```powershell
./gradlew.bat run
```
